# NPM configuration for React Native Lemonade App
# This file helps resolve dependency conflicts

# Use legacy peer dependency resolution to handle React version conflicts
legacy-peer-deps=true

# Use exact versions for more predictable builds
save-exact=true

# Increase timeout for slower CI environments
timeout=300000

# Disable funding messages to reduce CI noise
fund=false
